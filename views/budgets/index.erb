<h1>Budgets</h1>
<a href="/budgets/new">Create</a>
<table>

  <tr>
    <td><a href="/budgets/1">Overall Budget</a></td>
<td><%=@overall_budget.amount%></td>
  </tr>
  <% @tags.each do |tag| %>
  <tr>
    <td><a href="/tags/<%= tag.id %>"><%= tag.name%></a></td>
    <td><a href="/budgets/<%= tag.budget.id %>/edit"><%= tag.budget.amount %></a></td>
  </tr>
  <% end %>
</table>
