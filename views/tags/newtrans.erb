<h1>Transactions</h1>

<form class="" action="/tags/<%= @tag.id %>/newtrans" method="post">

  <input type="hidden" name="tag_id" value="<%= @tag.id %>">


  <div class="centre">
    <%= @tag.name %>
    <select class="" name="merchant_id" required>
      <% @merchants.each do |merchant| %>
      <option value="<%= merchant.id %>"><%= merchant.name %></option>
      <% end %>
    </select>
    <a href="/tags/<%= @tag.id %>/newmerch">new merchant</a>
  </div>

    <div class="centre">
      <label for="transaction-amount-field"> Amount: £
        <input type="number" step="0.01" name="amount" id = "transaction-amount-field" required>
      </label>
        <button type="submit" name="button">Add Transaction</button>
    </div>

</form>
